---
description: 代码分析输出规范，强制相对路径、可跳转代码块、调用链和模块依赖关系。适用于代码理解、排查问题、重构建议与实现说明。
alwaysApply: true
---

# 代码分析输出规范

当你在本项目中进行代码分析、定位问题、解释实现、给出重构建议或总结变更时，必须遵守以下输出规范。

## 输出要求

### 1. 优先使用相对路径

- 所有文件引用都应优先使用相对项目根目录的路径。
- 不要默认输出绝对路径。
- 只有在用户明确要求或需要说明系统路径时，才使用绝对路径。

### 2. 使用可跳转代码块引用

- 引用已有代码时，必须使用可跳转代码引用格式。
- 格式必须包含起始行、结束行与文件路径。
- 每个引用块中至少包含一行真实代码。
- 如果只需要指出某个位置，仍然尽量给出最小可定位的代码范围。

示例格式：

```12:28:packages/app/src/utils/request.ts
export function request() {
  // ...
}
```

### 3. 必须说明调用链

当你分析某段逻辑时，至少说明：

- 入口在哪个文件或组件
- 中间经过了哪些函数、方法或状态
- 最终影响了哪个结果或副作用

优先用简洁的链式描述，例如：

- `A.tsx -> useXxx() -> service.ts -> API`
- `button click -> handler -> store action -> render update`

### 4. 必须说明模块依赖关系

当涉及多个文件时，要说明它们之间的关系：

- 哪个模块依赖哪个模块
- 哪个模块是入口
- 哪个模块是公共能力
- 哪个模块只是被动消费方

如果依赖关系复杂，优先用列表或树状结构表达。

### 5. 先按前后端调用链路，再解释，再总结

推荐输出顺序如下：

1. 先给出前端或后端的入口文件和关键代码引用
2. 再按调用链路顺序展示对应代码块与可跳转文件路径
3. 每段代码块后面补一段精炼说明，说明它在链路中的作用，以及它具体做了什么
4. 最后给出结论、原因或建议

## 推荐模板

```text
定位
- `relative/path.tsx`
- `relative/path.ts`

调用链
1. `A.tsx`
```12:34:relative/path.tsx
...
```
作用说明：这段代码在调用链中的作用是……，它具体做了……

2. `B.ts`
```12:34:relative/path.ts
...
```
作用说明：这段代码在调用链中的作用是……，它具体做了……

模块依赖关系
- `A` 依赖 `B`
- `B` 依赖 `C`

结论
- 简短说明问题或建议
```

## 例外情况

- 如果用户只是在问概念，不涉及具体代码，可以只给概念性回答。
- 如果当前环境无法读取行号，也要尽量提供最接近的可定位文件引用。
- 如果代码是新建的而非已有代码，使用普通 Markdown 代码块，不要伪造行号引用。
