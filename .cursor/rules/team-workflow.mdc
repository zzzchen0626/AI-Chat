---
description: AI-Chat-Fe 仓库的团队工作流与代码评审指引。
globs:
  - 'packages/ai-chat-pc/src/**/*'
  - 'packages/ai-chat-plug/src/**/*'
  - 'packages/**/src/**/*'
alwaysApply: true
---

# AI-Chat-Fe 团队工作流

这个仓库是一个基于 pnpm 的前端 monorepo。请将修改视为生产级 UI 工作：保持改动聚焦，遵循现有约定，并在交付前验证行为是否正确。

## 1）开始修改前

- 先查看最小的相关范围：目标页面、组件、hook、store，以及紧密相关的工具函数。
- 修改前先阅读本地 Cursor 规则，尤其是关于依赖、Ant Design 使用方式、或代码分析输出的规则。
- 优先沿用现有模式，不要轻易引入新的抽象。
- 如果请求会影响多个包，请先识别包边界，并让每个包的修改保持独立。

## 2）实现习惯

- 保持改动最小，并与当前命名、布局和状态管理约定一致。
- 尽可能复用现有组件和工具函数。
- 保持 TypeScript 类型完整；除非有充分且可说明的理由，否则避免使用 `any`。
- 做 UI 相关修改时，保持 Ant Design 的使用方式与仓库其他代码一致。
- 解决需求时，避免顺手做无关重构。
- 如果改动涉及异步行为，要明确处理加载中、空状态和错误状态。

## 3）验证习惯

- 优先执行最窄范围内的验证：只对受影响的包运行定向 lint、类型检查或构建步骤。
- 如果是用户可见的改动，请结合组件树和状态流，手动在脑中检查渲染流程是否合理。
- 如果是 bug 修复，请确认原始失败路径和修复后的路径都成立。
- 新引入的 lint 问题优先立即修复，不要留到后面。

## 4）评审与交付检查清单

在结束任务前，请确认：

- 改动确实符合用户请求，没有修改无关文件。
- 现有 UI 模式被保留。
- 类型正确，没有引入明显的新警告。
- 加载态、禁用态、空状态和错误状态仍然合理。
- diff 容易评审，提交信息能够清楚表达意图。

## 5）团队沟通方式

在这个仓库协作时，建议按下面的结构汇报：

1. 明确准确范围。
2. 总结相关的现有行为。
3. 实现最小且安全的改动。
4. 验证结果。
5. 说明后续风险或假设。

## 6）推荐的 Cursor Team Kit 工作流模板

这个仓库的大多数任务建议按以下流程推进：

- **发现**：阅读目标文件及其附近依赖。
- **约束**：确认包边界和组件边界。
- **实现**：做最小且连贯的修改。
- **验证**：运行与受影响包相关的 lint / 类型检查 / 构建。
- **评审**：检查 diff 是否存在回归风险或可清理点。
- **交付**：准备简洁的问题说明、提交信息或 PR 描述。

## 7）仓库相关说明

- 这个仓库已经使用 pnpm 脚本和 monorepo 结构，因此优先使用包级命令。
- 保持 `.cursor/rules/` 中的项目规则与真实代码约定一致。
- 如果某个新约定反复出现，就把它沉淀成规则，方便后续保持一致。
