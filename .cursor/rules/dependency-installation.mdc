---
description: 依赖安装与缺失包处理规范
alwaysApply: true
---

# 依赖安装规则

- 当 agent 在分析、修改、运行或调试项目时，发现缺失依赖、未解析模块、构建失败、运行报错与安装依赖有关时，默认按依赖安装流程处理，不要先反问用户。
- 先读取并遵循 `dependency-installation` skill 中的流程，再决定是否执行安装。
- 如果依赖名称明确且属于项目内常规依赖，直接安装。
- 如果依赖名称不明确、涉及全局安装、涉及大版本升级、或会产生大范围变更，再向用户确认。
- 安装前先判断当前项目使用的包管理器，并使用对应命令。
- 安装后检查 manifest 和 lockfile 是否同步更新，并在适当时重新运行失败命令。
- 只有在安装会造成明显风险时才中断并询问。

## 默认行为

- 缺依赖导致的构建失败：自动处理。
- 缺依赖导致的运行失败：自动处理。
- 新增 import 对应的依赖明显缺失：自动处理。
- 仅凭猜测才可能需要的依赖：先确认再安装。

## 日常代码分析输出要求

当我要求你分析项目、模块、页面、功能链路、文件关系、调用流程或定位问题时，默认遵循以下输出方式，而不是每次都要求我额外补充提示词：

- 优先使用相对路径引用文件，例如 `src/components/Button/index.tsx`。
- 涉及代码片段时，优先用可跳转的代码引用格式输出。
- 解释模块关系时，尽量按文件路径、函数名、调用链、依赖关系组织答案。
- 如果需要展示代码，优先引用真实文件中的位置，而不是只给抽象说明。

## 解析优先级

1. 先定位相关文件
2. 再给出相对路径
3. 再给出可跳转代码块
4. 最后补充总结
